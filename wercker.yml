box: debian
build:
        steps:
                - arjen/hugo-build:
                        version: "0.14"
                        theme: hyde-x
deploy:
        steps:
                - lukevevier/gh-pages:
                        token: $GIT_TOKEN
                        basedir: public
                        repo: pmikkelsen/pmikkelsen.github.io
